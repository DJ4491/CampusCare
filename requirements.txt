# Core Django
Django==5.2.6
asgiref==3.9.1
sqlparse==0.5.3
tzdata==2025.2
django-compressor==4.4

# WSGI server for production
gunicorn==23.0.0

# Database (SQLite + SQLAlchemy support)
SQLAlchemy==2.0.36
alembic==1.14.1              # optional, for SQLAlchemy migrations
pysqlite3-binary==0.5.4   # only if you need latest SQLite features

# MySQL support (optional, safe to keep if needed later)
mysqlclient==2.2.7

# Serving static Files
whitenoise==6.6.0
